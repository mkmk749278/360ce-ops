fastapi==0.110.0
uvicorn[standard]==0.27.1
httpx==0.27.0
jinja2==3.1.3
itsdangerous==2.1.2
python-multipart==0.0.9
redis[asyncio]==5.0.8
# FCM HTTP v1 push (Phase 4) — mint the OAuth token from the service account.
# Deliberately google-auth (light) rather than the heavy firebase-admin SDK;
# the send itself goes over httpx, already a dependency.
google-auth==2.30.0
