__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.local
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.DS_Store

# Engine log pulls written by the diag runner (and by any test that
# exercises it). Untracked and unignored, this directory lands in `git add -A`
# on whichever branch happened to run the suite — the `.tmp` ledger artifact
# that conflicted on every merge from #839 to #845, one directory over.
logs/
.venv-ops/
